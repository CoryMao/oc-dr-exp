# MEMORY_WRITEBACK_RULES.md

## 1. 文件目标

本文件用于规定 `M_best` 组的 memory 写回细则。

本文件回答五个问题：

1. 什么时候写回
2. 先写到哪里
3. 哪些内容允许写回
4. 哪些内容禁止写回
5. `MEMORY.md` 应该如何更新

本文件是 `M_best` 组进行跨轮 memory 管理的直接操作规则。

---

## 2. 总体原则

写回的目标不是“尽可能多地保留信息”，而是：

> 只保留那些对下一轮任务真正有帮助、并且已经足够稳定的内容。

因此，本文件遵循四条总原则：

1. 先记录，再筛选，再晋升。
2. 先写 `daily memory`，后决定是否写 `MEMORY.md`。
3. 只写稳定信息，不写临时噪声。
4. 只写可复用信息，不写整份输出。

---

## 3. 写回时机

每轮任务完成后，必须执行一次写回检查。

写回检查固定发生在以下文件已经生成之后：

1. `outputs/runX_output.md`
2. `notes/feedback_runX.md`

只有在这两个文件都完成后，才允许更新 memory。

原因：

- 输出文件用于保留本轮结果
- 反馈卡用于筛选本轮错误与有效信息
- memory 写回必须建立在已完成的本轮判断之上

---

## 4. 写回顺序

每轮固定采用以下顺序：

1. 更新 `outputs/runX_output.md`
2. 更新 `notes/feedback_runX.md`
3. 更新 `memory/YYYY-MM-DD.md`
4. 判断是否更新 `MEMORY.md`
5. 若需要，再更新 `MEMORY.md`

不得跳过 `daily memory` 直接把信息写入 `MEMORY.md`。

---

## 5. 写回信息来源

允许进入写回判断的信息来源只有四类：

1. 当前任务文本
2. 当前输入材料
3. 本轮正式输出
4. 本轮反馈卡

如果某条内容不能明确追溯到以上四类来源之一，则不应写回。

---

## 6. daily memory 的写法

`memory/YYYY-MM-DD.md` 是工作记忆，不是长期记忆。

它允许记录：

1. 本轮任务是什么
2. 本轮主要结论是什么
3. 本轮出现了哪些正确点
4. 本轮出现了哪些错误点
5. 哪些内容建议晋升到长期 memory

推荐结构如下：

```md
# YYYY-MM-DD

## Run X

- 任务：
- 输入材料：
- 输出摘要：
- 关键正确点：
- 关键错误点：
- 建议写入长期 MEMORY 的内容：
- 不应写入长期 MEMORY 的内容：
```

daily memory 可以记录“观察”，但不应承担最终长期整理职责。

---

## 7. 允许写入 MEMORY.md 的内容

只有以下五类内容允许进入 `MEMORY.md`：

### 7.1 Project Scope

可写入：

- 当前项目研究什么
- 当前项目不研究什么
- 当前阶段任务边界

示例：

- 当前阶段只展开 memory 维度，不进入联合矩阵。

### 7.2 Current Stage

可写入：

- 当前实验进行到哪一步
- 当前主问题是什么

示例：

- 当前正在对 `M0` 与 `M_best` 进行最终配置落地。

### 7.3 Accepted Decisions

可写入：

- 已确认且后续默认沿用的实验决策
- 已确认的任务组织方式

示例：

- 最终 memory 对比只保留 `M0` 与 `M_best`。

### 7.4 Known Errors and Fixes

可写入：

- 已反复出现并被验证的错误
- 对应的稳定修正方式

示例：

- 不能把“论文主题相关”误写成“论文直接支持 claim”。

### 7.5 Evidence and Reporting Rules

可写入：

- 已确认的证据使用原则
- 输出时必须遵守的报告边界

示例：

- 矛盾文献必须显式呈现争议，不得强行统一。

---

## 8. 禁止写入 MEMORY.md 的内容

以下内容严禁写入 `MEMORY.md`：

1. 本轮完整输出
2. 大段推理过程
3. 未经验证的新猜测
4. 暂时性的措辞优化
5. 一次性的材料细节
6. 尚未确认是否正确的“经验”
7. 仅在单一 case 中成立、无法跨轮复用的局部判断

尤其禁止：

- 直接复制 `runX_output.md`
- 直接复制 `feedback_runX.md`
- 把某篇论文的具体结果全文搬进长期 memory

---

## 9. MEMORY.md 的更新方式

`MEMORY.md` 固定使用以下结构：

1. `Project Scope`
2. `Current Stage`
3. `Accepted Decisions`
4. `Known Errors and Fixes`
5. `Evidence and Reporting Rules`

更新时必须遵守：

1. 优先修改旧条目，而不是重复追加新条目。
2. 一条 bullet 只写一个稳定点。
3. 同一信息只保留一份最准确版本。
4. 若旧条目已过时，必须替换或删除，而不是并存。

---

## 10. 进入 MEMORY.md 的最低门槛

一条信息只有同时满足以下条件时，才可进入 `MEMORY.md`：

1. 已在本轮被明确观察到。
2. 已在反馈卡中被确认有价值。
3. 预计下一轮或后续轮次仍有用。
4. 不属于一次性任务细节。
5. 可以被压缩成稳定短条目。

只要有一条不满足，就不应写入长期 memory。

---

## 11. 写回判定流程

每轮收尾时，按以下顺序判断：

### Step 1

问自己：

- 本轮有没有形成新的稳定边界、稳定决策、稳定错误修正或稳定证据规则？

如果没有：

- 只更新 `daily memory`
- 不更新 `MEMORY.md`

### Step 2

如果有候选条目，再问：

- 这条内容是否属于五类允许写回内容之一？

如果不属于：

- 不写入 `MEMORY.md`

### Step 3

如果属于，再问：

- 这条信息是否已经在 `MEMORY.md` 中存在？

如果已存在：

- 优先修订旧条目

如果不存在：

- 再新增新条目

### Step 4

写完后再检查：

- 是否有重复条目
- 是否有临时性语言
- 是否把局部 case 细节误写成长期规律

---

## 12. 反馈卡与写回的关系

`feedback_runX.md` 是长期 memory 的筛选器。

只有反馈卡里被明确标为以下类型的内容，才可以进入 `MEMORY.md` 候选池：

1. 重复出现的正确边界
2. 重复出现的错误模式
3. 后续仍需保持的决策
4. 明确应长期遵守的证据规则

如果反馈卡没有明确支持，就不进入长期 memory。

---

## 13. 示例：哪些该写，哪些不该写

### 应该写入 MEMORY.md 的例子

- 最终 memory 对比只保留 `M0` 与 `M_best`。
- 矛盾文献必须保留分歧，不得强行统一。
- 不能把 benchmark 提升直接写成“达到类人能力”。

### 不应该写入 MEMORY.md 的例子

- Case 4 中 Paper A 的准确率是 57.7%。
- Run 2 的回答里用了某个句式，读起来更顺。
- 某次输出中一段尚未核验的解释。

前者更适合写在：

- `outputs/runX_output.md`
- `memory/YYYY-MM-DD.md`

而不是长期 `MEMORY.md`。

---

## 14. 执行结论

`M_best` 的跨轮写回应严格按下面一句话执行：

> 每轮先生成输出与反馈卡，再更新 daily memory，最后只把稳定、可复用、已确认的信息写入科研型 `MEMORY.md`。

如果不确定某条内容该不该写入长期 memory，默认不写。
